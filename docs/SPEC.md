# Go Hockey — спецификация (черновик)

Приложение для своих: один город, доступ через администратора.

## Роли

| Роль | Описание |
|------|----------|
| **admin** | Создаёт логины, запускает голосования и оплату, управляет составом |
| **player** | Участник по умолчанию после активации |

## Roster и игры (целевая модель)

Подробно: [ROSTERS-AND-VOTING.md](./ROSTERS-AND-VOTING.md).

- **Roster** — постоянный пул (как чат в Telegram): «Среда · ЛД Кристалл»; позже — «Пятница · ЛД Ногинск» и др.
- **Игра** — конкретная дата внутри roster; админ **запускает голосование** с **своими подписями** ответов.
- **Состав:** до **20 полевых** (`position = player`) + **резерв**; **2 вратаря** (`position = goalie`) — отдельный список, не в лимит 20.

## Группы (текущая реализация, этап 2)

- Пока: `day_groups` + `group_members` + `actual` (активация аккаунта).
- Будет миграция на roster + голосование по документу выше.

## Поток пользователя

### 1. Выдача доступа (админ)

1. Админ создаёт учётную запись: логин = **номер телефона**, генерируется **временный пароль**.
2. Админ отправляет игроку логин и пароль (вне приложения).

### 2. Первый вход (игрок)

1. Авторизация по телефону + паролю.
2. Обязательно: **сменить пароль** и задать **свой логин** (отображаемое имя).
3. После этого игрок **активен**; в текущей группе дня — `actual = true`.

### 3. Голосование (админ → игроки)

1. Админ **запускает голосование** на игру (дату) в roster.
2. Задаёт **подписи вариантов ответа** (например «Еду» / «Не еду», или свои).
3. Всем из пула roster — **уведомление** (push).
4. Голосование **активно** до `vote_ends_at`.
5. Состав по времени ответа «еду»:
   - полевые: первые **20** в игру, остальные резерв;
   - вратари: первые **2** в отдельном списке (не в счёт 20).

### 4. Управление составом (админ)

- Удалить из проголосовавших может **только админ**.
- При удалении — признак **не участвует**; список пересчитывается (очередь/резерв).

### 5. Оплата

1. Админ запускает этап **уведомлений об оплате**.
2. Игрок отмечает **«оплатил»**.
3. Админ видит статусы; для неоплативших — **повторная рассылка** уведомления.

### 6. Гости и очередь (админ)

- Админ может **добавлять гостей** (без обычной регистрации).
- Админ может **менять очередь** и **вставлять без очереди**.

---

## Этапы разработки (последовательно)

| # | Этап | Статус |
|---|------|--------|
| 0 | Дизайн-система + каркас PWA + экран входа | ✅ |
| 1 | Авторизация (телефон/пароль, смена пароля, логин) | ✅ |
| 2 | Группы по дням, список участников, `actual` | ✅ |
| 3 | Roster, админ: игроки, `position` player/goalie | — |
| 3b | Миграция БД: roster «Среда · Кристалл» | — |
| 4 | Голосование (подписи ответов), 20 + резерв + 2 GK, push | — |
| 5 | Удаление из состава админом | — |
| 6 | Оплата и повторные уведомления | — |
| 7 | Гости, ручная очередь | — |

## Дизайн

Референс UI: `black_orange.jpg` — тёмный неоморфизм, акцент `#FF6B00`, фон `#1E1E1E`.  
Логотип: `public/emblem.jpeg`.

## Инфраструктура (решение)

- **Хостинг reg.ru:** статика PWA (`dist/`) + API на **PHP + MySQL** (типично для shared-хостинга).
- **Масштаб:** до ~150 пользователей — MySQL на shared более чем достаточно.
- **Push:** Web Push (HTTPS + service worker + подписки в БД + отправка с сервера).
- **Админ:** только с телефона — достаточно адаптивного веб-интерфейса.
